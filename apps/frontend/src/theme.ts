import { createTheme } from '@mui/material/styles';

export const appTheme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#539dad',
        },
        background: {
            default: '#121212',
            paper: '#1e1e1e',
        },
    },
});
